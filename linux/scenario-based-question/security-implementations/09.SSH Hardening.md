# The Scenario
Since you correctly identified the SSH config file, let's put it into a real-world task. You are building a cluster of 100 web servers. You want to make them "SSH-Hardenend" by default.

# The Task
You need to disable **password-based logins** (forcing everyone to use SSH Keys) and change the **default SSH port** from 22 to 2222 (to reduce "noise" from basic bot scanners).

# The Question:

1. What are the two specific lines (directives) you need to change/add in `/etc/ssh/sshd_config` to achieve this?2. After you save the file, what command MUST you run for the changes to take effect?**

### The Answer:
``` Bash
default SSH port from 22 to 2222
add this in /etc/ssh/sshd_config
port 2222
PasswordAuthentication no
and i guess for forcing user to ssh key is
PubkeyAuthentication yes
and the do
sudo systemctl restart sshd
```