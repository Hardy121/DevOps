**The Scenario:**
You want to make sure the `sudoers` file is perfect. You decide to check it for errors without actually opening the file or trying to run a command. You know there is a "check" mode for the `visudo` command.

**The Question:**
1. What is the command to **check the syntax** of the sudoers file without editing it?

**The Answer:**
`visudo -c`